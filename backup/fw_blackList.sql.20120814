select '1',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = 'any' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '2',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '7' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '3',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port in('20','21') and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '4',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '22' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '5',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '23' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '6',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '111' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '7',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '135' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '8',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port in('137','138') and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '9',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '139' and fw.protocol != 'udp' and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '10',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '161' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '11',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '177' and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '12',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '445' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '13',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '513' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '14',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '514' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '15',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '514' and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '16',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '1433' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '17',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '1434' and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '18',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '1521' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '19',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '3306' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '20',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '3389' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '21',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '5432' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '22',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port < '6063' and fw.dst_port > '6000' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '23',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '7100' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '24',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = 'any' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '25',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '7' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '26',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port in('20','21') and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '27',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '22' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '28',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '23' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '29',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '111' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '30',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '135' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '31',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port in('137','138') and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '32',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '139' and fw.protocol != 'udp' and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '33',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '161' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '34',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '177' and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '35',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '445' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '36',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '513' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '37',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '514' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '38',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '514' and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '39',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '1433' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '40',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '1434' and fw.protocol != 'tcp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '41',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '1521' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '42',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '3306' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '43',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '3389' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '44',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '5432' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '45',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port < '6063' and fw.dst_port > '6000' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
select '46',ins.vsys_id,fw.id,fw.action from `instance` ins left join fw_policy fw on ins.vsys_id = fw.vsys_id left join `gip#instance` gi on fw.vsys_id = gi.vsys_id and fw.dst = gi.gip where gi.status in ('ATTACHED','DETACHED') and fw.src in ('any','0.0.0.0/0') and fw.dst_port = '7100' and fw.protocol != 'udp' and fw.from like '%INTERNET' and ins.vsys_id,fw.id,fw.action = 'Accept' order by vsys_id; 
